import type { QuizConfig } from "@/types/quiz";

// Questions and branching rules will be defined here when the quiz is implemented.
export const quizConfig: QuizConfig = { startQuestionId: "", questions: [], results: [] };
